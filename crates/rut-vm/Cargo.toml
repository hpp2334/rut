[package]
name = "rut-vm"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "the rut VM — self-managed RC heap (RFC 0016/0039), load-time verifier (0033 §2), interpreter with fuel+heap budgets (0034/0040)"

[lib]
name = "rut_vm"
path = "src/lib.rs"

[dependencies]
rut-core = { path = "../rut-core" }
rut-vm-threaded = { path = "../rut-vm-threaded" }
