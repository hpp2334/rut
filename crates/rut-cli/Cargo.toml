[package]
name = "rut-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "the `rut` binary — run / dump (RFC 0041 §2)"

[[bin]]
name = "rut"
path = "src/main.rs"

[dependencies]
rutc = { path = "../rutc" }
rut-core = { path = "../rut-core" }
