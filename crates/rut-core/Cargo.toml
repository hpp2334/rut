[package]
name = "rut-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "the rut VM — self-managed RC heap (0016/0039), typed bytecode loader+verifier (0033), interpreter with fuel+heap budgets (0034/0040)"

[lib]
name = "rut_core"
path = "src/lib.rs"
