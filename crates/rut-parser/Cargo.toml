[package]
name = "rut-parser"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "the rut parser — monotone cursor, no backtracking (RFC 0030 §4); flat-arena AST out"

[lib]
name = "rut_parser"
path = "src/lib.rs"

[dependencies]
rut-lexer = { path = "../rut-lexer" }
rut-ast = { path = "../rut-ast" }
rut-core = { path = "../rut-core" }
