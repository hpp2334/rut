[package]
name = "rut-driver"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "the pipeline driver — compile_module: parse → check/LIR → binary (RFC 0031→0033); the CLI and wasm entry point"

[lib]
name = "rut_driver"
path = "src/lib.rs"

[dependencies]
rut-core = { path = "../rut-core" }
rut-lexer = { path = "../rut-lexer" }
rut-ast = { path = "../rut-ast" }
rut-parser = { path = "../rut-parser" }
rut-lir = { path = "../rut-lir" }
