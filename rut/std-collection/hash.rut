// std:collection::hash — hashing and key comparison are ONE contract
// (RFC 0025/0026, RFC 0028): the interface Map/Set keys must satisfy.

pub interface Hashable {
    fn hash(self) -> u64;
    fn eq(self, other: Self) -> bool;
}
