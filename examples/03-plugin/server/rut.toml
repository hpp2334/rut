# server/ — the chat-room bus host pkg of 03-plugin (RFC 0035 §3): a
# pure declaration surface. The demo host (../src/lib.rs) binds the
# bodies over its `EventBus` opaque box; the plugin subscribes and
# emits through these two fns.
name = "server"
entry.type = "./server.d.rut"
